{
  "name": "langchain",
  "build": {
    "dockerfile": "../Dockerfile"
  },
  "workspaceFolder": "/app",
  "runArgs": [
    "-v", "${localWorkspaceFolder}:/app"
  ],
  "extensions": [
    "ms-python.python",
    "ms-toolsai.jupyter"
  ],
}
